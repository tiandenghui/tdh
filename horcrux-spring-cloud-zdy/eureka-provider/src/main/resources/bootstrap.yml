
server:
  port: 8200
spring:
  application:
    name: eureka-provider
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: dev
eureka.client.serviceUrl.defaultZone: ${client.serverUrl}

