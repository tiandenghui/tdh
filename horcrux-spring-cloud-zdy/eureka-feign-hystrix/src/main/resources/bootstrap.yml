spring:
  cloud:
    config:
      uri: ${config.server.uri.test}
      profile: dev
  application:
    name: eureka-feign-hystrix
  jmx:
    default-domain: eureka-feign-hystrix
server:
  port: 8010
